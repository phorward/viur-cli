//export const threads = state => state.threads

export const connected = (state) => {
	return state.connected;
};

export const started = (state) => {
	return state.started;
};

export const theme = (state) => {
	return state.theme;
};
